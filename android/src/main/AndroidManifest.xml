<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.reactnativebettertusclient">
  <application>
<!--    Service used for detecting when the app has been terminated -->
    <service
      android:enabled="true"
      android:name=".DetectAppStopService"
      android:exported="false"
      android:stopWithTask="false" />
    <service android:name=".CancelWorkService" android:enabled="true" />
  </application>

</manifest>
